# DPR Productivity Dashboard

Start with `PROJECT_HANDOVER.md` for operating, maintaining, and handing over this project.

Production start:

- Run with Gunicorn: `gunicorn app:server -c gunicorn.conf.py`
